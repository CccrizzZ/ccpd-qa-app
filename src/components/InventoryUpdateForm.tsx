import React, { useState } from 'react'
import { QARecord } from '../utils/Types'

const InventoryUpdateForm = () => {
  const [record, setRecord] = useState<QARecord>({} as QARecord)

  return (
    <div>

    </div>
  )
}

export default InventoryUpdateForm